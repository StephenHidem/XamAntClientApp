<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodels="clr-namespace:XamAntClientApp.ViewModels"
             x:DataType="viewmodels:HeartRateViewModel"
             x:Class="XamAntClientApp.Views.HeartRatePages.FeaturesPage">
    <ContentPage.Content>
        <StackLayout>
            <Grid Margin="10">
                <Frame BorderColor="Black" Margin="0, 10, 0, 0">
                    <StackLayout>
                        <!--<Picker ItemsSource="{Binding SportModeValues}" SelectedItem="{Binding ModeRequested}" Margin="4"/>-->
                        <Picker ItemsSource="{Binding SportModeValues}" Margin="4"/>
                        <Button Command="{Binding SetSportModeCommand}" Margin="4" Text="Set Sport Mode"/>
                    </StackLayout>
                </Frame>
                <Label Text="Sport Mode" Style="{StaticResource HeaderLabelStyle}"/>
            </Grid>
            <Grid Margin="10">
                <Frame BorderColor="Black" Margin="0, 10, 0, 0">
                </Frame>
                <Label Text="Features" Style="{StaticResource HeaderLabelStyle}"/>
            </Grid>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>